# 오전

류영표 강사

텍스트 마이닝
- 텍스트 수집하는 것으로 시작
- 사람마다 토큰의 의미가 다를 수 있음

가변 공간
- ex) list_3 = (3,), list_4 = (1,2) list_3+list_4 = (1,2,3) 이 코드가 가능한 이유는 list_3 3,뒤에 가변공간이 있기 때문
3,,의 경우 실행이 안됨.

# import numpy as np

#a=[] # 일반적인 리스트 계산
#for i in range(20):
#    if i%3==0:
#        a.append(i)
#print(a)

# [i for i in range(20) if i%3==0] # list compression

이력서 평가 시 - 인사팀에서 AI를 이용하여 키워드 요약 후 분석함.

현업에서는 Colab보다는 Visual Studio를 많이 씀

P(A|B) // baysian's rule = P(A B) / P(B)
B가 일어났을 때 A의 확률
